# Normalise line endings so the repo stays LF regardless of platform.
* text=auto eol=lf

# Blender files are binary - never touch them.
*.blend  binary
*.blend1 binary
*.exr    binary
*.png    binary
*.jpg    binary
